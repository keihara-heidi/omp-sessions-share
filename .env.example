# Local daemon config lives at ~/.omp/agent/omp-sessions-share.json (mode 0600).
# Run: bun run setup   (or omp-sessions-share-setup)
# Do not put host tokens, dashboard passwords, or cookie secrets in env files.
